#!/usr/bin/env ruby
require File.expand_path('../../lib/spelleken_van_mit',     __FILE__)

window = SpellekenVanMit::Window.new

begin
  window.show
rescue Interrupt => e
  puts "\nQuitting..."
  exit 1
ensure
  window.close
end
