#!/usr/bin/env ruby
require File.expand_path('../../lib/spelleken_van_mit', __FILE__)
begin
  require File.join(SVM::ROOT, 'config')
rescue LoadError
  require File.join(SVM::ROOT, 'config.example')
end

#                             resX resY fullscreen fps
$window = SVM::GameWindow.new(905, 600, false,     24.to_fps)
begin
  $stdout.puts "Welkom bij het Spelleken van mit v#{SVM::VERSION}. Enjoy!"
  $window.bootstrap
  $window.show
rescue Interrupt
  $stderr.puts "\nQuitting..."
  exit 1
ensure
  $window.close
  $stdout.puts String::EOL
end
